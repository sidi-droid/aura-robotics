# Scaffold Notice

Every directory in this repository other than `docs/` is currently **empty** and
contains only a `.gitkeep` file.

These directories are **not** evidence of existing work. They mirror the
repository structure specified in the AURA project proposal so that future code,
CAD, firmware, and simulation assets have agreed locations.

If you are picking this project up: read `README.md` for what actually exists,
and `docs/OPEN-QUESTIONS.md` for decisions that are still open.
